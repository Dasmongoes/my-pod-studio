{
  "buildCommand": "pnpm --filter @workspace/printflow run build",
  "outputDirectory": "artifacts/printflow/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api(.*)", "destination": "/api/index" }
  ]
}
